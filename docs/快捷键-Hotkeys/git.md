---
tags:
  - 快捷键
---
# Git 常用操作

| 关键词 | 命令 | 说明 |
|---|---|---|
| 清空缓存/取消跟踪 | `git rm -r --cached .` | 清空 Git 缓存（取消已跟踪的文件） |
| 删除版本控制文件 | `git rm <filename>` | 删除已列入版本控制的文件 |

> `.gitignore` 文件需放在仓库根目录下，与 `.git` 同级。
